<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.PresentationMode="ModalAnimated"
             Title="A Form"
             x:Class="UndoAssessment.Views.NewUserPage">
    <ContentPage.Content>
        <StackLayout Spacing="3" Padding="15">
            <Label Text="Name" />
            <Entry Text="{Binding Name}" />
            <Label Text="Age" />
            <!-- for simplicity -->
            <Entry x:Name="AgeEntry" Keyboard="Numeric" Text="{Binding Age}" />

            <!-- There as alternativy, may add <xct:TextValidationBehavior, but for it need install the Xamarin Community Toolkit package -->
            <!--<Entry x:Name="AgeEntry" Keyboard="Numeric" Text="{Binding Age}">
                <Entry.Behaviors>
                    <local:NumericValidationBehavior />
                </Entry.Behaviors>
            </Entry>-->

            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Submit" Command="{Binding SubmitCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>